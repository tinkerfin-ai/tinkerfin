# TinkerFin Studio Web 客户端

该目录包含 React 19 与 TypeScript 客户端，提供登录、会话列表、owned Run 的 AG-UI SSE、
Trace 历史与跟随、会话任务轨迹、Tool 状态、子 Agent 展示和 HITL 中断恢复。

完整体验步骤见 [Studio 上手指南](../../../docs/cn/studio/quick_start.md)。

## 本地运行

运行环境须满足以下任一 Node.js 版本范围：

- 20.x：20.19.0 或更高版本
- 22.x 及后续主版本：22.12.0 或更高版本

项目固定使用 pnpm 10.8.0。开发服务器默认把 `/api` 代理到
`http://127.0.0.1:8090`。

```bash
pnpm install --frozen-lockfile
pnpm dev
```

连接其他后端时可设置 API 基础地址：

```bash
VITE_API_BASE_URL=https://api.example.com pnpm dev
```

客户端会调用 `apps/studio/server` 提供的认证、数据库模型目录、会话、Trace、取消和
AG-UI 对话流接口。owned Run 网络中断后，客户端使用 canonical threadId、原 runId 和最后一条
Messaging 序号最多自动重连三次。建立实时连接等待响应头的期限为 45 秒，收到响应后不限制
流的持续时间。首响应丢失时保留原提交并提示任务状态尚未确认；失联后可点击“恢复连接”，
使用原 Run 和事件序号重新接收输出。重新打开页面时，已保存的当前 Run 自动恢复一次；
恢复耗尽后等待手动操作。断开连接不会调用停止接口，也不会推断后端任务已经取消。
没有本标签页提交记录的历史或 detached 会话只消费 Trace snapshot/follow，不重放 AG-UI 正文。
owned Run 进入终态后，客户端重新读取 Trace，并用权威视图替换临时 AG-UI 状态。
运行失败在对应轮次末尾显示“会话异常”，重新打开历史或接收实时失败均不弹 toast。仅明确发生在
执行前的初始化失败提供“重试”：使用当前模型、模式和上下文，将原问题及其附件引用发送为新一轮，
保留已有消息、输入草稿和另外选中的附件。失败属于运行，不改变用户消息状态，也不生成助手消息。
连接、附件上传和界面操作异常通过全局 toast 提示；手动恢复再次失败会产生新的提示。
输入字段的必填、格式和附件数量限制保留在对应输入区域。
终态后的历史同步失败保留已知运行结果，“重新加载”只读取历史，不重新提交已结束的 Run。
发送后立即清空文字输入；服务器在受理前明确拒绝请求时，若用户仍在同一会话且未编辑新草稿，
恢复原提交文字。网络失联不推断请求被拒绝，附件在收到受理事件前仍保留。

登录会话使用后端返回的 UTC `expires_at` 作为固定到期时间，不因页面操作或请求续期。
客户端会在定时到期、页面恢复和认证请求前检查该时间；到期或收到后端 401 时静默返回
登录页。页面启动校验遇到网络错误或服务端暂不可用时，会保持业务界面未挂载并自动重试，
不会清除仍可能有效的会话。一个标签页退出或到期后，其他同源标签页同步退出。

登录会话必须先成功写入 `localStorage` 才会进入内存状态。浏览器隐私策略、配额或存储异常
导致写入失败时，登录会被拒绝，页面显示可恢复提示；不会建立只在当前内存中有效的半会话。

侧栏账户区使用认证响应中的 `avatar_url` 显示头像；地址为空或图片加载失败时显示稳定的
名称首字符占位。账户菜单中的“设置”展示头像、展示名和用户名，并提供跟随系统、浅色、
深色三种外观选项。会话页头部只保留全局工作区操作，外观入口位于设置与登录页。

设置分为“账号管理”“通用”和“模型配置”。通用设置提供简体中文、English 和跟随系统三种语言偏好，
缺省使用简体中文，选择后立即作用于当前浏览器并同步同源标签页。跟随系统把所有中文变体
解析为简体中文，英语解析为 English，其他系统语言回退 English。语言切换只作用于前端
自有界面文案；后端响应、会话标题、消息、Plan、Todo、工具内容和代码始终保持原文。
语言偏好无法写入本地存储时，选择仍在当前页面生效，但刷新后不会保留。

模型配置按对话、生图用途分组，支持编辑、删除、直接设为默认与当前草稿测试。基础检查和能力测试分开，结果展示实际回复或图片；编辑配置后旧结果标为过期，保存单独提交。图片尺寸和格式使用表单控件，高级 JSON 编辑器展开时才加载，提供语法诊断和格式化；输入无效时保留草稿并阻止保存和测试。使用方法与测试费用边界见 [模型配置说明](../../../docs/cn/studio/quick_start.md#登录并配置模型)。

取消和保存按钮位于内容滚动区域外，滚动条只覆盖表单内容。展开测试或高级参数时，设置页自动露出新增内容；延迟加载的编辑器和测试结果使用相同行为。用户开始滚动、选择或输入后，视图保持用户选择的阅读位置。超过可见区域的长内容仍可正常滚动阅读。

模型保存和测试共用字段校验，错误显示在输入框下方并定位到首个错误字段，不显示浏览器原生气泡。模型设置与登录页复用同一表单反馈组件；减少动态效果时保留焦点提示，跳过抖动。返回或取消编辑会清除当前草稿、密钥输入和错误提示。

新会话在侧栏以“新会话”显示，首个 chat 请求发送空 `threadId`。客户端生成本次请求的
`runId`，并给 user 消息填写 `request-${runId}`，以满足标准 AG-UI 消息校验。后端不把
这个客户端消息 ID 作为业务身份；`RUN_STARTED` 只返回 canonical `threadId`、`runId` 与标题，
客户端保留当前临时消息。主终态后重新读取 Trace，并用其中的权威消息 ID 与正文替换临时状态。

## 输入与命令

消息输入卡片的底部工具行提供本地附件、Plan 状态、模型选择和发送或停止操作。模型选择
只展示后端目录中的可用模型，并随每次请求发送稳定模型 ID。
模型目录只返回用户可选择的模型信息；底层 Runtime 由服务端统一装配，客户端不参与选择。

输入 `/plan` 会在本地开启 Plan，不产生消息请求；输入 `/plan <消息>` 会开启 Plan，并只把
去掉命令前缀后的正文发送给后端。Plan 开启后，工具行显示黄色 `Plan` 状态按钮；该按钮是
关闭 Plan 的唯一入口，`/plan off` 不作为关闭指令执行。

附件入口支持 PNG、JPEG、WebP、GIF、PDF、DOCX 和 XLSX，最多 5 个，单个不超过
10 MiB、合计不超过 25 MiB。点击附件入口后，按钮先显示加载状态，再打开系统文件选择器；选择或取消后恢复入口。系统文件选择窗口的尺寸由浏览器和操作系统管理。
添加本地附件后立即进入待发送列表，上传中显示加载图标和进度，图片上传成功后显示缩略图。
附件上传使用浏览器原生 XMLHttpRequest 提供进度和取消，支持开发服务器的 HTTP/1.1 连接。
附件以单行显示，上传失败时保留附件，在文件名旁显示红色“上传失败”标识，可重试或移除。
文件名过长时截断显示，悬浮、键盘聚焦或点击文件名可查看完整名称；Escape 或点击外部可关闭提示。
只要有附件等待上传、正在上传或上传失败，发送按钮就不可用，Enter 也不会发送；所有附件就绪后才能发送。
模型不支持图片或图片能力未确认时，工具栏上方显示提示，可点击“切换模型”打开模型菜单；切换前保留草稿并禁止发送图片。
消息中的图片保持完整比例，无边框展示，悬浮或键盘聚焦显示引用和下载，触屏常显。
图片加载失败时，卡片内仅重试可用，引用和下载禁用；成功加载后恢复操作。
引用将附件加入当前草稿，保留已输入的文字；发送沿用附件标识。

点击图片打开应用内全屏预览，先显示已有预览，再加载原图。预览支持图片信息、下载、引用、
同组图片切换、适应窗口、原始尺寸、缩放与放大后拖动。方向键切图，Escape先收起信息，
再关闭预览并恢复焦点。原图或下载失败可在预览内重试。生成图片独立于工具字段详情，
展开和收起参数不会隐藏或重新加载图片；主工具与子Agent使用相同图片组件。

每次 start 与 resume 请求都使用 `forwardedProps.command.plan` 声明有效 Plan 状态：`on`
对应内部 `plan` 模式，`off` 对应内部 `default` 模式。HTTP 请求的模式字段只有
`forwardedProps.command.plan`。

会话消息保留浏览器原生滚动语义，并使用全局统一的视觉滑块。Trace 初始读取最新 100 个
Turn；存在 `historyCursor` 时，“加载更早消息”从同一固定 `asOfSeq` 再扩展 100 个 Turn。
已读取内容仍按 100 个可见条目分批展开并保持当前阅读位置。Trace follow 的实时增量不会进入
AG-UI 历史 reducer。

对话包含两条及以上已加载提问时，右侧提供“对话目录”。宽屏鼠标环境显示轮次刻度，
悬停或键盘聚焦可预览提问与公开回答摘要，点击跳转到提问；方向键和 Home/End 可选择入口，
Enter/Space 执行跳转，Escape 关闭预览。窄屏及触屏通过右侧目录按钮打开可滚动的提问列表。
刻度、预览标题及当前项使用浅深主题各自的品牌色。

目录只包含已加载内容，也能定位尚未显示在当前消息窗口中的提问；加载更早历史后自动补充。
跳转时暂停跟随新消息，点击“回到底部”恢复跟随。目录不会主动读取全部历史、发送消息或更改
输入草稿；思考、工具参数和子智能体内容不进入回答摘要。

链路页在会话运行中跟随节点变化；运行结束、等待审批或运行状态未知时只读取当前筛选快照，
不保持后台轮询。运行进入终态后会补取快照，并在权威会话详情到达后再次校准，保留已提交的
最后一批节点。同一会话和筛选的刷新保留已显示链路，读取完成后替换内容；首次读取或切换
查询范围时显示加载提示。节点状态与历史是否完整不用于判断会话是否仍在运行。

重新进入链路页，或浏览器窗口与标签页重新激活时，客户端读取当前会话详情；发现恢复或
新 Run 后自动继续实时更新。本标签页正在接收的对话流保持自己的状态，历史响应不会覆盖它。
同一持久化前缀的状态刷新保留已展开的历史窗口；会话前缀前进后采用新的权威窗口。
静态链路页在这些重新激活动作发生时检查其他标签页的变化；它不接收后台变化推送。
标签页隐藏、离开链路或切换会话会取消当前链路读取。读取失败保留重试入口，刷新会话失败时
提示重试并保留已知会话状态。

根 Agent 成功确认的 `write_todos` 在所属用户轮次显示为 `Todos`，Todo 内容只取根状态。
Composer 输入框或审批卡片上方的辅助操作行浮动显示：居中为「回到底部」，右侧为「任务轨迹 N」。
辅助操作显隐不改变对话可视高度或目录位置；任务
轨迹打开统一倒序树，同一时间只展开一个 Group，可从 Group 定位到对应用户消息。只有当前会话
请求任务轨迹，非当前 detached follow 显式跳过；切换会话时释放旧
Group/Todo 对象图。客户端不把任务轨迹数据写入 localStorage、sessionStorage 或服务端副本，
sessionStorage 只保存每个会话的抽屉开关偏好。

默认代理目标是 `http://127.0.0.1:8090`。需要临时连接其他本地端口时设置：

```bash
VITE_API_PROXY_TARGET=http://127.0.0.1:8092 pnpm dev
```

## 质量检查

```bash
pnpm test
pnpm test:proxy
pnpm lint
pnpm test:browser
```

HTTP 代理测试和浏览器测试使用 `@playwright/test@1.62.1` 与对应 Chromium。首次运行前安装浏览器：

```bash
pnpm exec playwright install chromium
```

`pnpm test:proxy` 会启动隔离的 HTTP/1.1 服务与 Vite 代理，通过真实浏览器验证附件字节、进度、鉴权、错误和取消，不访问实际后端或数据库。

`pnpm test:browser` 构建一次后，用两个工作进程运行普通交互测试，再串行运行标记为
`@performance` 的耗时、帧和内存测试。任一阶段失败即停止，保留该阶段的失败截图和 Trace；
控制台分别输出两个阶段的结果。

单项复测使用已有生产构建；修改源码后先重新构建。性能用例应单独使用一个工作进程：

```bash
pnpm build
pnpm exec playwright test tests/browser/conversation-failure.spec.ts
pnpm exec playwright test --grep @performance --workers=1
```

浏览器门禁覆盖 320、768、1024、1440px，浅色与深色主题、键盘焦点、touch/coarse pointer、
`prefers-reduced-motion`、forced colors 和页面级横向溢出。Vite 生产构建使用其 Baseline
Widely Available 默认目标；真实发布仍应按目标用户浏览器矩阵执行兼容性验证。

## 第三方许可证

仓库默认许可见[根 LICENSE](../../../LICENSE)。`pnpm build` 会在
`dist/third-party-licenses.md` 生成实际进入浏览器制品的依赖许可证清单。GSAP 当前仅用于 Studio
界面状态动效，适用边界以官方 Standard License 原文为准。

## 安全限制

当前客户端把 bearer token 保存在浏览器 `localStorage`，页面脚本可以读取该令牌，
因此不能直接作为生产认证边界。生产部署需改用 `HttpOnly`、`Secure`、`SameSite`
Cookie，并配套实现 CSRF 与 XSS 防护。

## 会话标题与并行连接

每个正在运行的会话拥有独立连接与恢复游标。切换或新建会话保持已有连接，停止和删除只影响指定会话。迟到的首帧或后台更新不会改变当前选择，浏览器历史导航也会释放草稿的页面选择权。

标题最多32个Unicode字符，默认截取输入前16个字符。自动标题通过原会话流的 `studio.conversation.title.updated` 自定义事件更新。标题独立按 `titleSeq` 合并，旧事件和旧历史响应不能覆盖用户命名；保存相同文字也表示固定标题。没有本地提交记录的设备继续读取历史和Trace快照，已在跟随的Trace连接不额外广播标题通知。
